@page
@model TorneoDepartamental.App.Frontend.Pages.Equipos.IndexModel
@{
}
<h1>Listado de Equipos</h1>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Nombre</th>
            <th scope="col">MunicipioId</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var equipo in Model.Equipos)
        {
            <tr>
                <td>@equipo.Id</td>
                <td>@equipo.Nombre</td>
                <td>
                @* @if(equipo.Municipio.Nombre == null){
                    <a class="btn btn-primary" asp-route-id="@equipo.Id">Asignar municipio</a>
                }else{ *@
                    @* <a class="btn btn-primary" asp-page="./Details" asp-route-id="@equipo.Id">Detalle</a>
                } *@
                <a class="btn btn-primary" asp-page="./Details" asp-route-id="@equipo.Id">Detalle</a>
                </td>
                
                
                
            </tr>
        }
    </tbody>
</table>
